a = 5
b = 3
sum = a + b
print("Sum:", sum)

#adding new code
def factorial(n):
   if n == 0:
       return 1
   else:
       return n * factorial(n-1)
print(factorial(5)) # Output: 120

adding some data in remote repo 
added new line in remote repo
